defaults:
  - base

project:
  version: v2
  description: "V1 + Pretrained ResNet50 backbone (ImageNet weights)"

model:
  pretrained: true
  siamese: false

loss:
  type: cross_entropy

dataset:
  tile_size: 384

augmentation:
  enabled: false
